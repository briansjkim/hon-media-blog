import React from 'react';

// will receive retrieved comments and display each comment w/ author and num of likes
const Comment = ({ comments }) => (
    <div>{comments}</div>
);

export default Comment;
